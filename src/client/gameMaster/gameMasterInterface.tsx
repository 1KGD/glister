import React from 'react';
import GameState from '../../common/gameState';
import './gameMasterInterface.css';

export default function GameMasterInterface({ state }: { state: GameState }): React.JSX.Element {
    return <>GM</>;
}